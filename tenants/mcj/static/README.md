# MCJ tenant static assets

Place a discipline logo here if it should differ from the shared KU mark.

Until then, `tenant.yaml` `logo_static` points at `images/KU_logo_2.png` in the app `static/` folder.
